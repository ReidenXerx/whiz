import WhizKit

// The entire executable. `@main` is sugar for calling `App.main()`, so the app
// itself can live in WhizKit where SwiftUI previews render and the test target
// can depend on it — see WhizApplication.
WhizApplication.main()
